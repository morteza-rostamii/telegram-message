<!--

#Run with
docker-compose up -d --build

# 1. Stop and remove old containers (optional but clean)
docker compose down

# 2. Pull new code from GitHub
git pull origin main  # or whichever branch

# 3. Build and start containers
docker compose up -d --build


-->
